# Session: P-6
Date: 2026-07-21
Agent: subagent (golanger — builder) + coordinator (PR + CI verification)
Piece / Brief: P-6 (docs/phases/P-6-cicd-ship.md)
Baseline SHA -> Head: 42de783 -> 71adf29

## Accomplished
- `.github/workflows/ci.yml` — 5-gate test workflow (one job each: `vet`, `build`, `test` (`-race`), `coverage` (LCOV + 80% floor), `govulncheck`), triggers on `pull_request`→master + `push`→master. `permissions: contents: read`; no secrets; no `pull_request_target`.
- `.github/workflows/deploy.yml` — `workflow_dispatch`-only (no auto-deploy), `environment: production`, Vercel CLI (`pull`/`build`/`deploy --prebuilt --prod`), tokens scoped per-step.
- `vercel.json` — `@vercel/go` builder against `cmd/server/main.go` + catch-all route (the ADR-0009 Go server preset; there is no literal `"framework":"go"` enum in Vercel — the `@vercel/go` builder is its concrete realization).
- `README.md` — one-liner, `/v1` endpoint table, embedded UI, local run + curl, CI/CD summary, human-setup checklist, metric quartet + no-backtracking guarantee.
- `.gitignore` — appended `.vercel/`.
- Every GitHub Action SHA-pinned (F-15): `actions/checkout@11bd719…` (v4.2.2), `actions/setup-go@3041bf5…` (v5.2.0), `actions/setup-node@39370e3…` (v4.1.0). `govulncheck ./...` ran clean (no vulnerabilities).

## Decisions made (and why)
- Pre-gate piece: no test-author/coverage/test-verifier (no new app tests; the CI workflow runs the existing suite). Recorded `test-coverage gate: skipped (pre-gate: CI/CD wiring + docs)`.
- One job per gate (not steps-in-one-job) → each surfaces as its own required status check, so branch protection can require each individually.
- `go.sum` legitimately absent: the module has zero third-party `require`s (pure stdlib), so no `go.sum` is generated; AC-3's "go.sum committed" is vacuous here, but `govulncheck` still gates (stdlib/toolchain CVEs) and becomes a real dependency gate the moment a dep is added.
- No durable/architectural decision surfaced → no ADR promoted.

## Deviations from the frozen plan
- None. The vercel.json shape (`@vercel/go` builder vs a literal `"framework":"go"`) is the concrete current realization of ADR-0009's "Go server preset" — the brief left the exact Go-on-Vercel shape to impl (AUDIT A1).

## Test evidence
- test-coverage gate: **skipped (pre-gate piece: CI/CD wiring + docs; no new app tests)**.
- Automated checks: `go build`/`go vet`/`go test ./...` still green (no Go changed).
- **CI verification (AC-2) — the real gate:** PR #1 (`phase/p-6-cicd-ship` → `master`) opened; GitHub Actions run **29845573253** completed **Success** (1m44s) with ALL 5 gates green: vet 22s, build 22s, **test (`-race`) 1m41s**, coverage(≥80%) 41s, govulncheck 24s. Evidence URL: https://github.com/NerdAlert58/sudoku-flow/actions/runs/29845573253 . (10 annotations = Node-20 deprecation notices on the pinned actions — non-blocking.)
- Compliance evidence: none (COMPLIANCE.md `Applicable hats: N/A`).

## Deployment evidence
- **Target:** manual (CONTEXT.md `cicd_deploy_hook: manual`). `deploy.yml` is `workflow_dispatch`-only → deploy is human-triggered by design (ADR-0016). Phase 5c local-deploy gate is skipped (CI-managed/manual).
- **Deploy command run:** none locally — the manual Vercel deploy is an operator step after Vercel setup.
- **Health check:** N/A here — verified post-deploy (README documents capturing `/v1/health` on the deployment for AC-6/AC-7).
- **Rollback command (unrun):** `vercel rollback ${LAST_DEPLOYMENT_URL}` (human-triggered).
- **Env vars propagated:** `VERCEL_TOKEN`/`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` (GitHub Secrets — operator adds).

## Leanness review
- Skipped (pre-gate piece: CI/CD wiring + docs, not app source).

## Acceptance status
- **AC-1** files exist — ✅. **AC-2** real PR CI run, all gates gating (run URL recorded) — ✅ VERIFIED. **AC-3** govulncheck gate (go.sum vacuous) — ✅. **AC-4** all actions SHA-pinned, no pull_request_target-with-secrets — ✅. **AC-7** README docs — ✅.
- **AC-5** deploy manual-gated (workflow_dispatch + `production` environment) — ✅ structurally; the required-reviewer RULE is an operator repo-settings step. **AC-6** vercel.json Go server preset — ✅ structurally; the LIVE deploy proving the preset serves `cmd/server` needs the Vercel setup.

## Open / next session — operator completion steps (README §Human setup)
1. Vercel: create/link the project, generate `VERCEL_TOKEN`, note `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID`.
2. GitHub: add the 3 secrets; enable branch protection on `master` requiring the ci.yml checks; configure the `production` Environment with a required-reviewer rule.
3. Merge PR #1 (or the phase chain) to master; run `deploy.yml` (manual) → capture the deployed `/v1/health` for the AC-6/AC-7 deploy half.
- Non-blocking test followups (prior jasnah RUBRIC_GAPs, future /nerdflow:cleanup): batch byte-cap branch; per-item parse-failure; CORS allowlist branch; cellInUnit guard; per-band band-hit floor.
