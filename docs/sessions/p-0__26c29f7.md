# Session: P-0
Date: 2026-07-20
Agent: subagent (golanger — test-author + builder)
Piece / Brief: P-0 (docs/phases/P-0-scaffold.md)
Baseline SHA -> Head: 26c29f7 -> 4607297

## Accomplished
- Go module `github.com/scottbushyhead/sudoku-flow` (`go 1.26`, `toolchain go1.26.5`, zero deps).
- `internal/sudoku`: `Grid` (`[81]uint8`, unexported cells, canonical `0`-blank `String()`), `Candidates` (`uint16` bitset, `math/bits`-backed, no-panic), `Parse` trust boundary with `%w`-wrapped sentinels `ErrInvalidLength` / `ErrInvalidCharacter` / `ErrDuplicateValue`.
- `internal/api`: `contract.go` (full frozen `/v1` surface + `{error,code}` envelope), `HealthHandler`, `MaxBytes` (delegates to `http.MaxBytesHandler`), `Recover` panic→500 middleware.
- `cmd/server/main.go`: `ServeMux`, `GET /v1/health` bound, listens on `$PORT` (no hardcoded port), `log/slog` JSON, `Recover(MaxBytes(routes))` chain.
- Tests (test-author): parse valid/`.`-alias/all-blank + length/char/duplicate typed errors + never-panics; health shape; MaxBytes over/under cap; Recover panic→500 envelope (added in re-dispatch round 1).

## Decisions made (and why)
- Grid cells unexported, `Parse` sole constructor — invariant lives at the trust boundary, not callers.
- `contract.go` declares the full `/v1` surface now (frozen comparison contract, ADR-0010) with primitive fields, no `internal/solver` import → P-0 compiles standalone.
- `MaxBytes`/`Candidates` kept despite single/zero P-0 callers — mandated by ARCHITECTURE contracts (MaxBytes is a testable seam; Candidates is P-1's solver surface).
- No durable/architectural decision surfaced → no ADR promoted.

## Deviations from the frozen plan
- `go test -race ./...` could not run on this Windows host (`-race` requires cgo; no C compiler installed). Ran non-race `go test ./...` (green). P-0 has zero goroutines / zero shared mutable state, so race detection is trivially green; the real `-race` gate runs in CI on Linux (P-6). Environment gap, not a code deviation. **Carries forward as a real risk for P-4 (concurrency) — a C toolchain or WSL/Linux is needed to run `-race` locally there.**

## Test evidence
- Red-state capture (Phase 5.0): tests failed at setup (no `go.mod` — builder's allow-list); test-author confirmed symbol-level red (`undefined: sudoku.Parse`, `undefined: api.HealthHandler`) in a throwaway module; gofmt-clean, no syntax errors.
- Green-state capture (Phase 5.1): `go test ./...` green — internal/api, internal/sudoku pass; cmd/server no test files. No tests deleted/skipped.
- Coverage report: coverage.lcov (lcov), 57.0% overall. Diff lines: `Parse`/`validateGivens`/`Grid.String`/`HealthHandler`/`writeJSON`/`MaxBytes`/`Recover` 100%. Uncovered (ruled reasonable P-0 exemptions by test-verifier): `Candidates` methods + `Grid.At` (P-1 solver surface, no P-0 caller/AC), `cmd/server/main.go` wiring (smoke-owned via AC-1/AC-5).
- Test-exempt lines applied: none.
- test-verifier verdict: FAIL (round 0) → PASS (round 1). Round-0 blocker: `Recover` panic→500 unverified. Routing: re-dispatched test-author to add `TestRecover_PanicBecomes500Envelope`; coverage 0→100%; re-verified PASS.
- Compliance evidence: none (COMPLIANCE.md `Applicable hats: N/A`).

## Deployment evidence
- **Target:** manual (CONTEXT.md `cicd_deploy_hook: manual`).
- **Deploy command run:** none — Phase 5c skipped.
- **Deploy exit code:** N/A.
- **Health check:** N/A — deploy skipped (CI-managed / manual deploy at P-6).
- **Rollback command (unrun):** N/A — deploy skipped.
- **Env vars propagated:** none (P-0 needs only `$PORT`, platform-provided).
- **Deviations:** none. `deployment: SKIP (cicd_deploy_hook: manual)`.

## Leanness review
- **RIGOR:** basic
- **Findings:** 1 delete (`internal/api/middleware.go:MaxBytes` — one-line pass-through over `http.MaxBytesHandler`, single caller).
- **Net removable:** -7 lines
- **Disposition:** advisory-only. Applying would break the test-author's locked `TestMaxBytes_*` tests (MaxBytes is a deliberate testable seam); the 7-line saving is not worth losing a mutation-sensitive body-cap test.
- **Raw report:**
    RIGOR: basic
    internal/api/middleware.go:L12: delete MaxBytes is a one-line pass-through wrapper over http.MaxBytesHandler with the same signature and a single caller (main.go run()); it owns no state (the cap value lives in main as maxBodyBytes). Delete the func and its 4-line doc comment; call http.MaxBytesHandler(routes(), maxBodyBytes) directly in run().
    net: -7 lines possible.

## Open / next session
- P-1 (solver core & singles) wires `POST /v1/solve` against the already-declared `contract.go` types; `Grid`/`Candidates` ready for the technique ladder.
- Plan-level followup (from test-verifier RUBRIC_GAP): P-0's AC set had no criterion gating panic recovery though the brief mandated the middleware. Consider adding such an AC pattern for middleware in future phase briefs (a `/nerdflow:impl` concern, not a build edit).
- Pre-P-4: resolve local `-race` (install a C toolchain or run in WSL/Linux) so the concurrency race gate is meaningful before batch/parallelism work.
